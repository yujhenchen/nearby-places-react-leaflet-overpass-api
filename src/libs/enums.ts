export enum Category {
    restaurant = 'restaurant',
    library = 'library',
    school = 'school',
    park = 'park',
}

export enum PositionType {
    userCurrent = 'user',
    newPosition = 'new'
}
