export enum Category {
    restaurant = 'restaurant',
    school = 'school',
    park = 'park',
}

export enum PositionType {
    default = 'default',
    userCurrent = 'user',
    newPosition = 'new'
}
